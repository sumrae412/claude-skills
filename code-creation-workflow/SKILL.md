---
name: code-creation-workflow
description: ARCHIVED — Use /claude-flow instead. Predecessor superseded by claude-flow v3 (progressive-disclosure phase files, memory injection, tiered review).
---

# Code Creation Workflow (Archived)

This skill has been superseded by `/claude-flow` (v3). The `scripts/` and `references/` subdirectories are retained as experimental material (causal, moe_router, rag, registry, symbolic_verifier) but the orchestration flow lives in `claude-flow/`.
