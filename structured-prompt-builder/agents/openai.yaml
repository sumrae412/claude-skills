interface:
  display_name: "Structured Prompt Builder"
  short_description: "Create reliable LLM prompts"
  default_prompt: "Use $structured-prompt-builder to turn this goal into a reliable prompt template."
policy:
  allow_implicit_invocation: true
