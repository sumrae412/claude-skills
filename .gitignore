# Python caches (generated by test runs inside skill scripts)
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.benchmarks/
.mypy_cache/
**/.venv/
**/node_modules/

# OS
.DS_Store

# Claude Code harness state — embedded agent worktrees, not skill content.
# `git add -A` would otherwise pick these up as submodule gitlinks (mode 160000).
.claude/
